## Summary

<!-- What does this change do, and why? -->

## Changes

<!-- Bullet the notable changes. New tool? Update the README tool table + docs/tool-catalog.md. -->

-

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] `npm test` passes
- [ ] Device-touching behavior verified manually on a booted simulator (paste evidence below)
- [ ] No `as any` / `@ts-ignore`; no shell execution introduced (all commands via `lib/exec.ts`)
- [ ] README / `docs/` updated if tools or behavior changed

<!-- Paste real command output / tool results for device-touching changes -->
